<div align="center">

<!-- System Schematic Banner -->
<img src="./assets/banner.svg" width="100%" alt="dharshan-X System Schematic Banner" />

</div>

<br/>

## // 01. RUNTIME CONFIGURATION

A compiler-focused systems engineer designing high-throughput backend runtimes and compiler intermediate representations. Optimizing for deterministic latency and strict memory layouts.

```toml
# dharshan.toml - Developer Runtime Settings
[system]
name = "Dharshan"
role = "Systems & Backend Engineer"
location = "Salem, Tamil Nadu, India"
language = "Rust // C++ // C"

[compiler_internals]
targets = ["wasm32-wasi", "x86_64-unknown-linux-gnu"]
optimizations = ["mir-inliner", "dead-code-elimination", "register-allocation"]
ir_passes = ["ssa", "cfg-simplification", "loop-vectorization"]

[platforms]
kernel = "Linux"
virtualization = ["Docker", "Kubernetes"]
pipeline = ["GitLab CI", "GitHub Actions"]
```

<br/>

## // 02. PIPELINE PASSES &amp; DOMAINS

A technical register of engineering domains, core architectures, and stack alignments.

### Frontend Compiler Passes
* **AST Generation** - Building lexers, parsers, and custom syntax trees for domains.
* **Semantic Analysis** - Type inference, scope validation, and name resolution.
* **Intermediate Representation** - Translating AST into SSA (Static Single Assignment) form.

### Virtual Machines &amp; Runtimes
* **Stack Machines** - Implementing custom WebAssembly interpreters and runtime executors.
* **Memory Management** - Designing lock-free allocators, slab allocators, and cache-aligned layouts.
* **POSIX API Bindings** - Exposing system calls, file descriptors, and socket abstractions.

### Systems Infrastructure
* **Containerization** - Packaging environments into minimal, reproducible, scratch-based containers.
* **CI Pipelines** - Automating parallel build, test, lint, and target verification pipelines.
* **Kubernetes Operators** - Automating complex state machines and system controllers.

<br/>

## // 03. ACTIVE CRATES &amp; PROJECTS

Monospace register of active codebases and systems repositories.

| Registry | Description | Stage |
| :--- | :--- | :--- |
| `wasm-vm` | Minimal WASI runtime implemented in pure Rust. | `Beta` |
| `ast-compiler` | Custom compiler frontend with optimization passes. | `Pre-alpha` |
| `slab-alloc` | Cache-aligned slab allocator for real-time systems. | `Stable` |
| `sys-bpf` | Linux eBPF telemetry dashboard and filter utilities. | `Active` |

<br/>

## // 04. NETWORK PORTS

Active socket listeners and address coordinates.

* **Email** : you@email.com
* **LinkedIn** : [dharshan-X](https://linkedin.com/in/dharshan-X)
* **Twitter** : [dharshan-X](https://twitter.com/dharshan-X)
